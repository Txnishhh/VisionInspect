# VisionInspect
**VisionInspect** is a modular Python tool automating shape detection and classification to replace manual inspection. Using OpenCV, it preprocesses images, isolates contours, and calculates geometric metrics (area, perimeter, vertices) to classify shapes like circles, squares, and triangles. It outputs JSON data and annotated images via a CLI.
